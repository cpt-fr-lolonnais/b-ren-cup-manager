

## Plan: Video-Datei in public/ kopieren

Die hochgeladene Datei `BC-IntroMovie.mp4` wird in das `public/`-Verzeichnis des Projekts kopiert, damit sie direkt über URL erreichbar ist (z.B. `/BC-IntroMovie.mp4`).

### Schritt
1. Kopiere `user-uploads://BC-IntroMovie.mp4` nach `public/BC-IntroMovie.mp4`

Keine Code-Änderungen nötig — die Datei wird einfach abgelegt.

